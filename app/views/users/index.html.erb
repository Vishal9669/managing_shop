
<h1><%= link_to "Create new Users", new_user_path, class: "btn btn-primary" %></h1>
<h1><%= link_to 'Back to dashboard', root_path %></h1>
<br>
<h1 style="color:red">Users List</h1>

<table class="table table-bordered">
  <tr>
    <th>Id</th>
    <th>FirstName</th>
    <th>LastName</th>
    <th>Email</th>
    <th>ContactNo</th>
    <th>Address</th>
    <th>Gender</th>
    <th>Edit</th>
    <th>Delete<th>
  </tr>
<% @users.each do |user| %>
  <tr>
    <td><%= user.id%></td>
    <td><%= link_to user.first_name, user %></td>
    <td><%= user.last_name %></td>
    <td><%= user.email %></td>
    <td><%= user.contact_no %></td>
    <td><%= user.address %></td>
    <td><%= user.gender %></td>
    <td><%= link_to "Edit", edit_user_path(user.id) %></td>
    <td><%= link_to "Destroy", user_path(user.id),
    method: :delete,
    data: {confirm:'Are you sure ?'} %></td>
    </a>
  </tr>
<% end %>
</table>
