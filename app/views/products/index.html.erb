<h1><%= link_to "Add new Product", new_product_path, class: "btn btn-primary" %></h1>
<h1><%= link_to 'Back to dashboard', root_path %></h1>
<br>
<h1 style="color:red">Products List</h1>


<table class="table table-bordered">
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Edit</th>
    <th>Delete<th>
  </tr>
  <% @products.each do |product| %>
    <tr>
      <td><%= product.id%></td>
      <td><%= link_to product.name, product %></td>
      <td><%= link_to "Edit", edit_product_path(product.id) %></td>
      <td><%= link_to "Destroy", product_path(product.id),
      method: :delete,
      data: {confirm:'Are you sure ?'} %></td>
      </a>
    </tr>
  <% end %>
</table>

